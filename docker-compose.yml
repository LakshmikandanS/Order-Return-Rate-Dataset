version: '3.8'

services:
  supplymind-intelligence:
    build: .
    ports:
      - "8000:8000"  # Executive Dashboard
      - "8001:8001"  # AI Inference Engine
    volumes:
      - ./data:/app/data
      - ./models:/app/models
